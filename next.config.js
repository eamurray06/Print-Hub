/**
 * @type {import('next').NextConfig}
 */
const nextConfig = {
  experimental: {
    // Enable the experimental app directory (used below for the page component)
    appDir: true,
  },
};

module.exports = nextConfig;
